#pragma once
#include <QObject>

class EcuReader : public QObject {
    Q_OBJECT
public:
    explicit EcuReader(QObject* parent = nullptr);
    Q_INVOKABLE void connectToDevice() {}
signals:
    void errorChanged(const QString& msg, int code = 0);
};
